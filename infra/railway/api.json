{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "Dockerfile",
      ".dockerignore",
      "infra/docker/**",
      "infra/railway/api.json",
      "package.json",
      "bun.lock",
      "bunfig.toml",
      "turbo.json",
      "apps/api/**",
      "packages/db/**",
      "packages/domain/**",
      "packages/jobs/**",
      "packages/config/**"
    ]
  },
  "deploy": {
    "region": "europe-west4-drams3a",
    "startCommand": "/app/entrypoint.sh api",
    "preDeployCommand": ["/app/entrypoint.sh migrate"],
    "preDeployTimeoutSeconds": 600,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "drainingSeconds": 30,
    "overlapSeconds": 10
  }
}
