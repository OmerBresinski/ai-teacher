// Moved to `@tj/slides` (ADR 0025 §9): the theme catalogue, projector floors and FIT_VERSION. Re-exported here so every
// `../model/themes` import in the editor keeps resolving.
export * from "@tj/slides/themes";
