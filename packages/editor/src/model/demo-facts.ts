// Moved to `@tj/slides` (ADR 0030 item 4) with the recipe tests that build from it. Re-exported
// here so every `../model/demo-facts` import in the editor keeps resolving.
export { DEMO_FRACTIONS_FACTS, DEMO_LESSON_FACTS } from "@tj/slides/worksheet";
