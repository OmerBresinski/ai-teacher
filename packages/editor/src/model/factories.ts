// Moved to `@tj/slides` (ADR 0025 §9): ids, rich-doc builders and the slide/lesson factories. Re-exported here so every
// `../model/factories` import in the editor keeps resolving.
export * from "@tj/slides/factories";
