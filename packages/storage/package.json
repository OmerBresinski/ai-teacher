{
  "name": "@tj/storage",
  "version": "0.0.0",
  "private": true,
  "description": "StorageAdapter implementations for Teaching Journey: local disk (development/tests) and S3-compatible via Bun.S3Client (production, Railway Bucket), plus createStorage(env) and deleteByPrefix (ADR 0026, F15-R02).",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@tj/domain": "workspace:*"
  },
  "devDependencies": {
    "@tj/config": "workspace:*",
    "typescript": "5.9.3"
  }
}
