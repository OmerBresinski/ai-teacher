# GENERATED from infra/env.contract.ts by `bun run env:generate` -- do not edit by hand.
#
# Extends gitleaks' default rules with one rule per secret-scoped variable of the environment
# contract: a literal `NAME=<20+ chars>` for any of them fails the pre-commit hook
# (`gitleaks protect --staged`) and the CI `secrets` job. Examples, docs and tests are allowed to
# show placeholders; the compose credentials (postgres/postgres) are synthetic and allowed.
# Vendored agent skills (`.agents/skills/`, ADR 0017) carry upstream example strings, never ours.
title = "Teaching Journey"

[extend]
useDefault = true

[allowlist]
description = "Synthetic local values and documentation"
paths = [
  '''\.env\.example$''',
  '''^docs/env\.md$''',
  '''\.test\.tsx?$''',
  '''\.spec\.ts$''',
  '''(^|/)\.agents/skills/''',
]
regexes = [
  '''postgres://postgres:postgres@(localhost|host\.docker\.internal|127\.0\.0\.1)''',
  '''\$\{\{[^}]+\}\}''',
  '''(process|import\.meta)\.env\.''',
]

[[rules]]
id = "tj-database-url"
description = "Teaching Journey secret DATABASE_URL assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bDATABASE_URL\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["DATABASE_URL"]

[[rules]]
id = "tj-test-database-url"
description = "Teaching Journey secret TEST_DATABASE_URL assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bTEST_DATABASE_URL\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["TEST_DATABASE_URL"]

[[rules]]
id = "tj-better-auth-secret"
description = "Teaching Journey secret BETTER_AUTH_SECRET assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bBETTER_AUTH_SECRET\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["BETTER_AUTH_SECRET"]

[[rules]]
id = "tj-resend-api-key"
description = "Teaching Journey secret RESEND_API_KEY assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bRESEND_API_KEY\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["RESEND_API_KEY"]

[[rules]]
id = "tj-google-client-secret"
description = "Teaching Journey secret GOOGLE_CLIENT_SECRET assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bGOOGLE_CLIENT_SECRET\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["GOOGLE_CLIENT_SECRET"]

[[rules]]
id = "tj-microsoft-client-secret"
description = "Teaching Journey secret MICROSOFT_CLIENT_SECRET assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bMICROSOFT_CLIENT_SECRET\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["MICROSOFT_CLIENT_SECRET"]

[[rules]]
id = "tj-s3-access-key-id"
description = "Teaching Journey secret S3_ACCESS_KEY_ID assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bS3_ACCESS_KEY_ID\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["S3_ACCESS_KEY_ID"]

[[rules]]
id = "tj-s3-secret-access-key"
description = "Teaching Journey secret S3_SECRET_ACCESS_KEY assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bS3_SECRET_ACCESS_KEY\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["S3_SECRET_ACCESS_KEY"]

[[rules]]
id = "tj-aws-bearer-token-bedrock"
description = "Teaching Journey secret AWS_BEARER_TOKEN_BEDROCK assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bAWS_BEARER_TOKEN_BEDROCK\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["AWS_BEARER_TOKEN_BEDROCK"]

[[rules]]
id = "tj-pexels-api-key"
description = "Teaching Journey secret PEXELS_API_KEY assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bPEXELS_API_KEY\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["PEXELS_API_KEY"]

[[rules]]
id = "tj-turbo-token"
description = "Teaching Journey secret TURBO_TOKEN assigned a literal value (infra/env.contract.ts)"
regex = '''(?i)\bTURBO_TOKEN\b[ \t]*[:=][ \t]*["']?([A-Za-z0-9+/_=.:@-]{20,})["']?'''
keywords = ["TURBO_TOKEN"]
